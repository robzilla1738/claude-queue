{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "claude-queue",
  "version": "1.1.1",
  "description": "Queue follow-up tasks in a clickable terminal UI; the running Claude session finishes its current task, then automatically picks up the next queued task — even while idle, as long as the queue window is open.",
  "author": {
    "name": "Robert Courson"
  },
  "homepage": "https://github.com/robzilla1738/claude-queue",
  "license": "MIT",
  "keywords": ["queue", "tasks", "stop-hook", "tui", "workflow"]
}
